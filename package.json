{
  "name": "dsh-stability-audit",
  "version": "0.1.0",
  "description": "Scan installed DeepSeek Harness plugins and grade stability risk (hook surface, startup work, preflight health, packaging, dependencies). 扫描已安装插件的稳定性风险（钩子面/启动任务/预检/打包/依赖）。",
  "type": "module",
  "main": "index.js",
  "files": [
    "index.js",
    "lib",
    "cordis.patch.yml"
  ],
  "scripts": {
    "test": "node --test test/",
    "lint": "node --check index.js && node --check lib/scanner.js && node --check lib/report.js"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "license": "MIT",
  "keywords": [
    "dsh",
    "dsh-plugin",
    "stability",
    "audit",
    "plugin-health"
  ]
}